## ls-sync

`ls` files under the `dir` synchronously.

### Install

    npm i ls-sync --save

### Usage

```
var ls = require('ls-sync');

console.log(ls('.'));
console.log(ls('..'));
```

### License

MIT